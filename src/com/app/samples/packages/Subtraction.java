//Adding another class to the package : packages
package com.app.samples.packages;

//Adding another class to the package : packages
public class Subtraction {
	
	//static method to return result of subtraction
	public static double sub(double a, double b) {
		return (a-b);
	}
}
